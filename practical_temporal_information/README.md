# Practical temporal-information benchmarks

**Branch:** `agent/practical-temporal-information-benchmarks`

## Purpose

Develop a fourth, deliberately grounded paper that translates the temporal-information resource program into standard detector physics and explicit falsification tests. The three mature theorem/proof stacks remain separate and scientifically frozen.

Working title:

> **Operational temporal-information benchmarks for photodetection**

## Current gate status

- WP07 prior-art/significance: **PASS WITH NARROWED CLAIMS**.
- WP08 final pre-manuscript: **PASS**.
- WP09 first hostile manuscript audit: **PASS AFTER SPECTATOR GENERALIZATION**.
- WP10 second hostile scientific/build audit: **PASS AFTER R2/R3 REPAIRS**.
- WP11 exact R3 artifact/render audit: **PASS**.
- R4 presentation-only freeze: **ACTIVE**; no scientific changes permitted.

## Current scientific center

Use a selected carrier/sideband pair separated by `hbar Omega` inside a stationary baseline

`rho_p = a_p |c><c| + p |s><s| + sigma_p`,

with `[rho_p,H]=0`, stationary inert spectators, `a_p>p`, and `a_p->q>0` as `p->0+`. The sideband seed `p` is an incoherent/phase-randomized population seed; a fixed coherent seed is outside the exact theorem.

For a calibrated local converter acting only on the selected pair,

`P_s(p;r)=p+(a_p-p)sin^2(kappa r)`,

and

`R_lin^2 = a_p p/[kappa^2(a_p-p)^2]`.

The frozen flagship finite-radius theorem specializes exactly to

`(R_lin^2/4) Tr F <= p`,

because its endpoint projector is the projector supporting the range of the selected tangent, `P_U,Omega=|s><s|`, not a global spectral-tail projector.

At zero seed,

`Delta P_s(0)=4 kappa^2 q`,

so the Paper-4 proposition is

**`lim_(p->0+) 4p/R_lin^2 = 4 kappa^2 q = Delta P_s(0)`.**

The limit is insensitive to allowed stationary spectator populations and the detailed normalization-compensation path, provided the selected carrier occupation tends to `q` and the converter leaves spectators inert.

A valid completed equatorial POVM attains

`Tr F=4 q kappa^2=Delta P_s(0)`.

## Novelty boundary

Do **not** claim novelty for finite Fisher information generated by a quadratically vanishing probability/eigenvalue at a boundary. Gefen--Rotem--Retzker (2019) and Safranek (2017) already cover that general mechanism.

Candidate distinct content is narrower:

- controlled finite spectral seed;
- exact finite affine physical radius;
- radius-normalized continuation into the empty-sideband curvature;
- independent measurement/falsification architecture connecting baseline/tangent tomography, second-order sideband curvature, and phase-sensitive Fisher information.

Priority remains **unverified, not certified**.

## Standard detector-facing content

### Linear Gaussian bridge

`Tr F/T = 2/NEP(f)^2`

under the locked one-sided-PSD / peak-quadrature convention.

### Poisson timestamps

`Tr F/T=lambda_0`, with independent timing jitter multiplying by `|Phi_J(Omega)|^2`.

### Conventional-specification incompleteness

Two detectors can have the same DC NEP and same responsivity `3 dB` bandwidth yet differ by

`13/3 ~= 4.33`

in Fisher information at the nominal bandwidth because their noise spectra differ. This is an illustrative standard-physics counterexample, not a novelty theorem.

### Type-II memory benchmark

The same homogeneous saturation curve `r=lambda exp(-lambda m)` can coexist with different timestamp information. At the common maximum deterministic recovery is uniquely information-singular. This theorem belongs to the frozen companion random-time paper and is only translated into a practical model-discrimination protocol here.

### Resonant implementation benchmark

For equal-frequency resonant modes,

`H_0=hbar nu(N_C+N_S)`,

and the two-boson fixed shell gives

`V_min=8(gt)^2=(1/2)Tr C`,

`A_ex=hbar nu V_min`.

The exchange operators commute with `H_0`; unequal bare frequencies require an explicit pump/controller for global energy conservation. This is a benchmark of the frozen companion implementation theorem, not a new beam-splitter theorem.

## Noncircular falsification protocol

The support crossover is compared using independent data products:

1. baseline + local tangent tomography -> `R_lin`;
2. zero-seed second-order sideband-population fit -> `Delta P_s(0)`;
3. separate phase-sensitive record -> Fisher matrix.

Always distinguish:

- **Level I:** detector/state/model reduction failure;
- **Level II:** resource-law challenge only after independent verification of theorem hypotheses;
- **Level III:** failure of an ideal saturating equality/model.

## Deterministic manuscript chain

`draft -> R1 mechanical -> R2 theorem -> R3 hostile-review -> R4 presentation`

- R1: removes REVTeX-incompatible falsification table/list machinery only.
- R2: replaces only the support-crossover section with the stationary-spectator theorem and operational protocol.
- R3: changes only three stale references plus the equal-frequency/unequal-frequency resonant benchmark clarification.
- R4: adds only `\\hypersetup{hidelinks}` to remove visible PDF hyperlink rectangles.

Every layer has an exact isolation gate.

## Verified R3 build

Run `32684317367`, job `97306449423`: all gates PASS.

Artifact `9505151860`, archive digest

`sha256:f6463ec3928f873acc2e5b11b964c71093337f8ca901b0eff511274bc7aa044b`.

Exact R3 PDF:

- 8 pages;
- 266067 bytes;
- SHA-256 `b1816af4811e900f05fa56eac4141d16ed617441f52c855b94eae6b571b475af`.

All eight pages were rendered at 180 dpi and inspected: no clipping, overlap, broken glyphs, black squares, or equation overflow. The only presentation issue was default hyperlink borders, hence R4.

## Immediate work

1. finish R4 CI/artifact/render verification;
2. close disposable PR #35 unmerged;
3. synchronize all root landing files to the exact R4 freeze;
4. then begin the four planned publication figures and publication compression/submission packaging.

Do not reopen theorem expansion before the R4 freeze is closed.

## Authoritative notes

- `notes/WP07_PRIOR_ART_AND_SIGNIFICANCE_GATE.md`
- `notes/WP07A_CLOSE_PRIOR_ART_BOUNDARY_SUPERRESOLUTION.md`
- `notes/WP08_FINAL_PREMANUSCRIPT_STACK_AND_SPEC_INCOMPLETENESS.md`
- `notes/WP09_HOSTILE_MANUSCRIPT_AUDIT_AND_SPECTATOR_INDEPENDENT_CROSSOVER.md`
- `notes/WP10_R2_BUILD_AND_SECOND_HOSTILE_AUDIT.md`
- `notes/WP11_R3_RENDER_AUDIT_AND_R4_PRESENTATION_FREEZE.md`

## Documentation rule

Every material advance must update the relevant note and handoff. When the frontier changes, also update root `README.md`, root `AGENTS.md`, `ROADMAP.md`, and `docs/CURRENT_RESEARCH_STATE.md`.
