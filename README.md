# The Universal Photodetection Resource Problem

**Status synchronized:** 2026-08-23

The repository is authoritative; chat history is not.

## Mature papers — preserve separately

1. PRX Quantum flagship — *Two spectral-resource regimes for autonomous temporal information*.
2. Broad random-time/timestamp spectral-information paper.
3. PRA dynamical completion — *Exact minimum unitary coupling cost of prescribed rank-changing quantum-state curvature*.

The active fourth program is:

`agent/practical-temporal-information-benchmarks`

Working title:

> **Operational temporal-information benchmarks for photodetection**

Provisional journal target: **Physical Review Applied**.

## Paper 4 — R4 frozen

The practical manuscript has passed two hostile scientific audits, exact deterministic source-isolation gates, CI compilation, warning checks, and full-page render inspection.

Current publication-facing freeze: **R4**.

Final verification:

- workflow run `32684526293` — PASS;
- job `97307019940` — PASS;
- artifact `9505218922`;
- archive digest `sha256:9905a2cbd4366d57731fc8f4a99c6f72a513629a8727257a43131e02efb96cce`;
- PDF: 8 pages, 266068 bytes;
- PDF SHA-256 `794cb1c52326dc1965e14ea8ccd15530b41b2e523ca501e88f081cf69d741a01`.

All eight pages were rendered at 180 dpi and inspected. No clipping, overlap, broken glyphs, black squares, equation overflow, unresolved citations/references, or overfull boxes remain. R4 differs from scientifically frozen R3 only by `\\hypersetup{hidelinks}`; a direct render diff changes only the former hyperlink-border rectangles.

Disposable PR #35 was closed **unmerged**. There are currently zero open PRs.

## Principal candidate Paper-4 result

Select carrier/sideband free-Hamiltonian modes separated by `hbar Omega` and use the stationary baseline

`rho_p=a_p|c><c|+p|s><s|+sigma_p`,

with `[rho_p,H]=0`, stationary inert spectators, `a_p>p`, `a_p->q>0`, and an incoherent/phase-randomized sideband population seed `p`.

For a calibrated lossless converter acting only on the selected pair,

`P_s(p;r)=p+(a_p-p)sin^2(kappa r)`,

`R_lin^2=a_p p/[kappa^2(a_p-p)^2]`.

The frozen flagship finite-radius theorem specializes exactly to

`(R_lin^2/4)Tr F<=p`.

At zero seed,

`Delta P_s(0)=4kappa^2 q`,

hence

**`lim_(p->0+)4p/R_lin^2=4kappa^2 q=Delta P_s(0)`.**

A completed equatorial POVM attains

`Tr F=4qkappa^2=Delta P_s(0)`.

The practical falsification protocol reconstructs `R_lin` from baseline/tangent tomography, `Delta P_s(0)` from an independent zero-seed second-order fit, and the Fisher matrix from a separate phase-sensitive measurement.

## Novelty boundary

Do **not** claim novelty for finite Fisher information generated by quadratically vanishing probabilities/eigenvalues at a boundary. Gefen--Rotem--Retzker (2019) and Safranek (2017) already establish that general mechanism.

Candidate distinct content is the controlled finite-seed finite-radius continuation

`4p/R_lin^2 -> Delta P_s(0)`

and the integrated detector-facing falsification architecture.

Priority remains **unverified, not certified**.

## Standard detector bridge

For peak optical-power quadratures under the locked one-sided-PSD convention,

`Tr F/T=2/NEP(f)^2`.

Ideal fractional Poisson timestamps give `Tr F/T=lambda0`; independent timing jitter multiplies by `|Phi_J(Omega)|^2`.

An explicit colored-noise example shows two detectors can have identical DC NEP and identical responsivity `3 dB` bandwidth while differing by `13/3≈4.33` in Fisher information at that nominal bandwidth.

## Companion-derived practical benchmarks

### Detector memory

For generalized iid Type-II recovery, fixing mean recovery fixes the complete homogeneous saturation curve

`r=lambda exp(-lambda m)`

but not timestamp information. At the common maximum deterministic recovery is uniquely information-singular. This theorem belongs to the frozen random-time companion; Paper 4 only turns it into a practical model-discrimination protocol.

### Dynamical implementation

For equal-frequency resonant modes,

`H_0=hbar nu(N_C+N_S)`,

the fixed two-excitation beam-splitter benchmark gives

`V_min=8(gt)^2=(1/2)Tr C`,

`A_ex=hbar nu V_min`,

with unchanged total bare-energy distribution. Unequal bare frequencies require an explicit pump/controller for global energy conservation. This is a benchmark of the frozen PRA theorem, not a new beam-splitter theorem.

## Falsification discipline

Paper 4 distinguishes:

1. **Level I:** detector/state/model reduction failure;
2. **Level II:** resource-law challenge only after independent verification of theorem hypotheses and resource quantities;
3. **Level III:** failure of a model-specific saturating equality.

## Current work order

The theorem/manuscript expansion phase is closed.

**Next: WP12 publication figures.** Create at most four deterministic figures:

1. same conventional specs, different FI spectra;
2. same Type-II saturation, different timestamp information;
3. support-seed survival→synthesis crossover;
4. equal-frequency resonant implementation + falsification/calibration map.

After figure integration, rebuild/render/hostile-review the manuscript, then perform publication compression and fresh APS policy/submission checks.

## Claim discipline

No prize-level framing. No implied experimental validation without data. No novelty claim for standard NEP, generic Fisher sensing, Poisson/dead-time formulas, random/variable dead time, interval characterization, paralyzable correlations, sideband generation/metrology, seeded/vacuum interferometry, generic boundary-QFI behavior, beam-splitter physics, or standard interferometry.

## Frozen companion publication packages

PRXQ R4: run `32674844366` PASS; artifact `9502376602`.

PRA R1: run `32673160217` PASS; artifact `9501942180`.
